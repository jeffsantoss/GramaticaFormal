import java.util.Scanner;

public class Main {

	public static void main(String[] args) {

		
		 new Janela();
		 /*
		Grammar grammar = null;
		String input;
		
		Scanner scanner = new Scanner(System.in);
		
		System.out.println("Input: ");
		input = scanner.nextLine();
		scanner.close();
		
		try {			//nonTerminalStates | initialState | terminalStates | productionRules
			grammar = new Grammar("S A B C", "S", "a b c d", "SA Sa ABb Ab BcC Bc CS Cd");
		} catch (InvalidGrammarException ige) {
			System.out.println(ige.getErro());
			ige.printStackTrace();
		}
		
		System.out.println(grammar.input(input) ? "Valid!" : "Invalid!");
		*/
	}

}
